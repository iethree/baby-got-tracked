{
  "name": "baby-got-tracked",
  "version": "0.1.0",
  "description": "track your baby's bodily functions!",
  "main": "index.js",
  "scripts": {
    "css:watch": "TAILWIND_MODE=watch NODE_ENV=development postcss .bgt.css -o .bgt.bundle.css -w --verbose",
    "css:build": "TAILWIND_MODE=build NNODE_ENV=production npx postcss ./bgt.css -o .bgt.bundle.css --verbose"
  },
  "author": "RPL",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.10",
    "postcss-import": "^12.0.1",
    "postcss-nested": "^5.0.5",
    "tailwindcss": "^2.1.1"
  },
  "devDependencies": {
    "postcss-cli": "^8.3.1"
  }
}
